<!-- Файл: Views/PieChartControl.xaml -->
<UserControl x:Class="RepairServiceAppMVVM.Views.PieChartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <ItemsControl x:Name="PART_ItemsControl" ItemsSource="{Binding Slices, RelativeSource={RelativeSource AncestorType=UserControl}}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Path Fill="{Binding Fill}" ToolTip="{Binding Title}">
                        <Path.Data>
                            <PathGeometry>
                                <PathFigure StartPoint="150,150" IsClosed="True">
                                    <LineSegment Point="{Binding Points[0]}"/>
                                    <!-- ИСПРАВЛЕНО: Привязываем IsLargeArc -->
                                    <ArcSegment Point="{Binding Points[1]}" Size="150,150" SweepDirection="Clockwise" IsLargeArc="{Binding IsLargeArc}"/>
                                </PathFigure>
                            </PathGeometry>
                        </Path.Data>
                    </Path>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>